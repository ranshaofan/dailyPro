/* page/index/index.wxss */
@import "../common/util.wxss";
/* 基础变量与重置 */
.icon-keyboard{
  /* ===== 可配置参数 ===== */
  --size: 50rpx;
  --color: rgba(169,132,103);

  width: var(--size);
  height: var(--size);
  display: inline-block;

  background-color: var(--color);

  -webkit-mask-repeat: no-repeat;
  -webkit-mask-position: center;
  -webkit-mask-size: contain;
}
.icon-voice{
  /* ===== 可配置参数 ===== */
  --size: 44rpx;
  --color: rgba(169,132,103);

  width: var(--size);
  height: var(--size);
  display: inline-block;

  background-color: var(--color);

  -webkit-mask-repeat: no-repeat;
  -webkit-mask-position: center;
  -webkit-mask-size: contain;
}
.icon-add{
  /* ===== 可配置参数 ===== */
  --size: 54rpx;
  --color: rgba(169,132,103);

  width: var(--size);
  height: var(--size);
  display: inline-block;

  background-color: var(--color);

  -webkit-mask-repeat: no-repeat;
  -webkit-mask-position: center;
  -webkit-mask-size: contain;
}
.icon-send{
  /* ===== 可配置参数 ===== */
  --size: 50rpx;
  --color: rgba(169,132,103);

  width: var(--size);
  height: var(--size);
  display: inline-block;

  background-color: var(--color);

  -webkit-mask-repeat: no-repeat;
  -webkit-mask-position: center;
  -webkit-mask-size: contain;
}
.icon-close{
  /* ===== 可配置参数 ===== */
  --size: 32rpx;
  --color: #666;
  width: var(--size);
  height: var(--size);
  display: inline-block;
  background-color: var(--color);
  -webkit-mask-repeat: no-repeat;
  -webkit-mask-position: center;
  -webkit-mask-size: contain;
}
.icon-del{
  /* ===== 可配置参数 ===== */
  --size: 64rpx;
  --color: #666;
  width: var(--size);
  height: var(--size);
  display: inline-block;
  background-color: var(--color);
  -webkit-mask-repeat: no-repeat;
  -webkit-mask-position: center;
  -webkit-mask-size: contain;
}
.icon-right{
  width: 40rpx;
  height: 40rpx;
  display: inline-block;
  background-color: #A98467;
}
.icon-book{
  background-color: rgba(255, 255, 255, 0.7);
  width: 40rpx;
  height: 40rpx;
  margin-right: 12rpx;
}
.icon-arrow{
  /* ===== 可配置参数 ===== */
  width: 50rpx;
  height: 50rpx;
  display: inline-block;
  background-color: rgb(230, 230, 230);
}
.icon-edit{
  width:32rpx;
  height: 32rpx;
  background-color: rgb(148, 148, 148);
}

.rightWrap{
  background-color: #fff;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 50%;
  width: 50rpx;
  height: 50rpx;
  margin-right: 12rpx;
}

page {
  background-color: #FAFAF9;
  font-family: "Nunito", -apple-system, sans-serif;
  color: #44403C;
  height: 100vh;
}

.container {
  height: 100vh;
  display: flex;
  flex-direction: column;
}

.shadow-card {
  box-shadow: 0 4px 20px rgba(0,0,0,0.03);
}

.shadow-float {
  box-shadow: 0 12px 30px -8px rgba(169, 132, 103, 0.4);
}

/* Header - 整体上移：减少顶部 padding */
.header {
  padding: 5rpx 40rpx 20rpx; /* 顶部从80rpx减为40rpx */
  background-color: rgba(250, 250, 249, 0.9);
}

.header-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30rpx; /* 压缩间距 */
}

.title-row {
  display: flex;
  align-items: baseline;
  gap: 16rpx;
}

.title-main {
  font-size: 68rpx;
  font-weight: 800;
}

.title-sub {
  font-size: 32rpx;
  color: #A8A29E;
  font-weight: 600;
}

.status-badge {
  font-size: 26rpx;
  color: #A98467;
  margin-top: 8rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.icon-button {
  width: 88rpx;
  height: 88rpx;
  background: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-button image {
  width: 60rpx;
  height: 60rpx;
}

.stats-card {
  background: white;
  border-radius: 48rpx;
  padding: 18rpx 40rpx;
  display: flex;
  align-items: center;
  justify-content: space-around;
  margin-top: 10rpx;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.stat-value {
  font-size: 52rpx;
  font-weight: 800;
}

.stat-label {
  font-size: 24rpx;
  color: #A8A29E;
}

.divider-v {
  width: 2rpx;
  height: 60rpx;
  background: #F5F5F4;
}

/* Content Scroll */
.content-scroll {
  flex: 1;
  overflow: hidden;
}

/* Status Section - 修复遮挡 */
.status-section {
  padding: 20rpx 0 10rpx 40rpx;
  height: 160rpx;
}
.section-row{
  justify-content: space-between;
  display: flex;
  padding: 0px 7rpx;
}
.section-label {
  font-size: 24rpx;
  font-weight: 700;
  text-transform: uppercase;
  color: #A8A29E;
  margin-bottom: 10rpx;
  display: block;
}
.section-label2 {
  font-size: 24rpx;
  font-weight: 700;
  text-transform: uppercase;
  color: #A8A29E;
  display: block;
  margin-left: 40rpx;

}
.status-tags-scroll {
  display: flex;
  flex-direction: row;
  white-space: nowrap;
  /* padding-bottom: 10rpx;  */
  height: auto;
  min-height: 110rpx;
}

.status-tag {
  display: inline-flex; /* 关键：确保水平排列不换行 */
  align-items: center;
  padding: 12rpx 36rpx;
  margin-right: 20rpx;
  border-radius: 40rpx;
  flex-shrink: 0;
  transition: all 0.2s ease;
  height: 100rpx; /* 统一高度 */
  box-sizing: border-box;
}

/* 激活状态：深色 */
.status-tag.active {
  background-color: #5D5752;
  color: white;
}

/* 普通状态：白色 */
.status-tag.normal {
  background-color: white;
  border: 1px solid #F5F5F4;
  color: #44403C;
}

.tag-icon-img {
  width: 44rpx;
  height: 44rpx;
  flex-shrink: 0;
}

.tag-text {
  display: flex;
  flex-direction: column;
  margin-left: 14rpx;
}

.tag-title {
  font-size: 28rpx;
  font-weight: 700;
  line-height: 1.2;
}

.tag-desc {
  font-size: 20rpx;
  opacity: 0.8;
  margin-top: 4rpx;
}

.empty-tips {
  font-size: 26rpx;
  color: #A8A29E;
  padding: 20rpx 0;
}

/* Timeline - 时间轴整体上移并留出底部空间 */
.timeline-main {
  padding: 10rpx 40rpx 120rpx; /* 增加底部留白 120rpx */
  position: relative;
}

.timeline-line {
  position: absolute;
  left: 86rpx;
  top: 40rpx;
  bottom: 120rpx;
  width: 4rpx;
  background: repeating-linear-gradient(to bottom, #E7E5E4 0, #E7E5E4 12rpx, transparent 12rpx, transparent 24rpx);
  opacity: 0.6;
}

.timeline-item {
  display: grid;
  grid-template-columns: 88rpx 1fr;
  gap: 24rpx;
  margin-bottom: 50rpx;
  position: relative;
  z-index: 1;
}

.timeline-icon-wrap {
  display: flex;
  justify-content: center;
  padding-top: 8rpx;
}

/* .timeline-icon {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: white;
  box-shadow: 0 0 0 16rpx #FAFAF9;
} */
.timeline-icon {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 0 0 12rpx #FAFAF9; /* 稍微缩小阴影圈，让视觉更精致 */
}
.event-card {
  background: white;
  border-radius: 48rpx;
  padding: 36rpx;
  position: relative;
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.time-range {
  font-size: 30rpx;
  color: #A8A29E;
  font-weight: 500;
}

.category-badge {
  display: flex;
  align-items: center;
  gap: 15rpx;
  padding: 4rpx 30rpx;
  border-radius: 40rpx;
}
/* 确保分类标签文字和点点垂直对齐 */
.category-badge text {
  font-size: 22rpx;
  font-weight: 700;
}

/* 让空隙按钮点击时有缩放效果 */
.gap-button:active {
  transform: scale(0.98);
  background-color: #F5F5F4;
}

.dot { width: 16rpx; height: 16rpx; border-radius: 50%; }

.event-title {
  font-size: 34rpx;
  /* font-weight: 700; */
  margin-bottom: 12rpx;
  display: block;
}

.event-desc {
  font-size: 26rpx;
  color: #78716C;
  line-height: 1.6;
}

/* Colors */
.bg-orange { background-color: #EAC6A8; }
.bg-orange-soft { background-color: rgba(234, 198, 168, 0.1); }
.text-orange { color: #D99F80; }

.bg-blue { background-color: #B5C7D3; }
.bg-blue-soft { background-color: rgba(181, 199, 211, 0.1); }
.text-blue { color: #8DA9C4; }

/* Gap Style */
.timeline-dot-wrap {
  display: flex;
  justify-content: center;
  padding-top: 32rpx;
}

.timeline-dot-small {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background-color: #E7E5E4;
  box-shadow: 0 0 0 16rpx #FAFAF9;
}

.gap-button {
  border: 4rpx dashed #E7E5E4;
  border-radius: 48rpx;
  padding: 32rpx 40rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.gap-action {
  font-size: 28rpx;
  color: #A8A29E;
  font-weight: 600;
  letter-spacing: 4rpx;
}
.gap-label{
  background-color: #f0f0f0;
  width: 80rpx;
  height: 50rpx;
  display: inline-flex;
  justify-content: center;
  align-items: center;
  border-radius: 30%;
  margin-left: 20rpx;
  color: #969290;
  font-size: 25rpx;
  letter-spacing: 4rpx;
}
.kongxianadd{
  background-color: #E7E5E4;
  border-radius: 50%;
  width: 70rpx;
  height: 70rpx;
  display: flex;
  justify-content: center;
  align-items: center;
}
.add-icon { background-color: #fff; width: 50rpx; }
.action-btn {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  position: absolute;
  /* background-color: #ccc; */
  /* border-radius: 50%; */
  right: 30rpx;
  bottom: 38rpx;
}
.btn-hover {
  opacity: 0.8;
  transform: scale(0.97);
}

/* Floating Action Button */
.fab-button {
  position: fixed;
  right: 48rpx;
  bottom: 60rpx; /* 调整位置，避免遮挡内容 */
  width: 110rpx;
  height: 110rpx;
  background-color: #A98467;
  color: white;
  border-radius: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  border: 6rpx solid rgba(255,255,255,0.4);
}

.material-icons {
  font-size: 44rpx;
}
.iconCateWidth{
  width: 30rpx;
  height: 30rpx;
  margin: 0;
}

/* page/index/index.wxss */

/* page/index/index.wxss */

/* 1. 最外层容器：确保位置和间距 */
/* page/index/index.wxss */

/* 1. 底部容器位置不变 */
/* page/index/index.wxss */

/* 1. 底部容器：确保位置处于页脚上方 */
/* page/index/index.wxss */

.bottom-action-container {
  position: fixed;
  bottom: 30rpx;
  left: 30rpx;
  right: 30rpx;
  display: flex;
  align-items: center;
  z-index: 1000;
}

/* 1. 一体化长条底座 */
.input-main-part {
  flex: 1;
  height: 110rpx;
  box-shadow: inset 0 0 20rpx rgba(169,132,103);
  background: #FFFFFF; 
  border-radius: 55rpx;
  display: flex;
  align-items: center;
  margin-right: 25rpx;
  
  /* 柔和的大范围阴影 */
  /* box-shadow: 0 12rpx 40rpx rgba(0, 0, 0, 0.06); */
  border: 1rpx solid rgba(0, 0, 0, 0.03);
}

/* 2. 分割竖线 */
.divider-line {
  width: 2rpx;
  height: 40rpx;
  background-color: rgba(169,132,103,0.5); 
  margin: 0 10rpx;
}

/* 3. 左侧切换按钮 */
.input-toggle {
  width: 100rpx;
  height: 100rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #706B65;
}

/* 4. 中间输入区（透明底色） */
.main-input-area {
  flex: 1;
  height: 100%;
  display: flex;
  align-items: center;
}

/* 语音按钮（无底色版） */
.voice-btn-flat {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #706B65;
  font-size: 30rpx;
  font-weight: 500;
  transition: background 0.2s;
  border-radius: 0 55rpx 55rpx 0; /* 仅右侧圆角 */
}

/* 文字模式（无底色版） */
.text-input-wrap-flat {
  width: 100%;
  display: flex;
  align-items: center;
  padding: 0 30rpx 0 10rpx;
}

.input-box {
  flex: 1;
  font-size: 28rpx;
  color: #44403C;
}

/* 5. 独立的右侧按钮 */
.standalone-add-btn {
  width: 110rpx;
  height: 110rpx;
  box-shadow: inset 0 0 20rpx rgba(169,132,103);
  background: #FFFFFF;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #706B65;
  flex-shrink: 0;
  /* box-shadow: 0 12rpx 40rpx rgba(0, 0, 0, 0.06); */
  border: 1rpx solid rgba(0, 0, 0, 0.03);
}

/* 交互反馈 */
.active-tap {
  background-color: #F9F8F7; /* 点击时淡淡的反馈 */
}

.material-icons {
  font-size: 46rpx;
}

/* 弹窗 */
/* 1. 遮罩层：控制背景淡入淡出 */
/* 遮罩层初始状态：全透明，隐藏 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 2000;
  background: rgba(0, 0, 0, 0); 
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  
  /* 关键：使用 visibility 配合 opacity，确保组件存在但不可见 */
  visibility: hidden;
  transition: background 0.3s ease, visibility 0.3s;
}

/* 激活状态：背景变黑，变为可见 */
.modal-mask.show {
  visibility: visible;
  background: rgba(0, 0, 0, 0.4);
}

/* 弹窗内容初始状态：完全沉在屏幕下方 (100%) */
.modal-content {
  background: #FFFFFF;
  border-top-left-radius: 64rpx;
  border-top-right-radius: 64rpx;
  padding: 40rpx 60rpx 60rpx;
  max-height: 85vh;
  position: relative;
  
  /* 关键：动画定义 */
  transform: translateY(100%);
  transition: transform 0.3s cubic-bezier(0.165, 0.84, 0.44, 1); /* 这种曲线滑入更高级 */
}

/* 激活状态：向上滑入原位 */
.modal-mask.show .modal-content {
  transform: translateY(0);
}

/* --- 下面是内部组件样式，保持不变 --- */
.modal-handle {
  width: 80rpx; height: 8rpx; background: #E5E2E0; border-radius: 4rpx; margin: 0 auto 30rpx;
}
.modal-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 40rpx; }
.modal-title { font-size: 40rpx; font-weight: 800; color: #2D2A26; }
.close-btn { width: 72rpx; height: 72rpx; background: #F5F5F4; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: #A8A29E; }
.info-cards { display: flex; gap: 24rpx; align-items: center; margin-bottom: 50rpx; }
.time-card { flex: 1.2; background: #F8F7F5; border-radius: 40rpx; padding: 40rpx 30rpx; }
.time-box { display: flex; align-items: center; justify-content: center; gap: 16rpx; }
.time-val { font-size: 44rpx; font-weight: 700; color: #44403C; }
.category-tag-large { flex: 0.8; height: 120rpx; background: #4A453E; color: #FFFFFF; border-radius: 40rpx; display: flex; align-items: center; justify-content: center; font-weight: 600; box-shadow: 0 10rpx 25rpx rgba(74, 69, 62, 0.2); }
.text-area-container { border: 1rpx solid #F0EEEB; border-radius: 48rpx; padding: 40rpx; }
.content-textarea { width: 100%; font-size: 32rpx; color: #44403C; }
.modal-footer { display: flex; gap: 24rpx; margin-top: 60rpx; }
.delete-btn { width: 120rpx; height: 120rpx; background: #F8F7F5; border-radius: 40rpx; display: flex; align-items: center; justify-content: center; color: #A8A29E; }
.confirm-btn { flex: 1; height: 120rpx; background: #A98467; color: #FFFFFF; border-radius: 48rpx; display: flex; align-items: center; justify-content: center; font-size: 34rpx; font-weight: 700; box-shadow: 0 15rpx 30rpx rgba(169, 132, 103, 0.3); }
.word-count{
  color: rgb(185, 185, 185);
  background-color: rgb(241, 241, 241,0.7);
  width: 80rpx;
  padding: 0rpx 10rpx;
  display: flex;
  height: 60rpx;
  border-radius: 20%;
  justify-content: center;
  align-items: center;
}
.driveline{
  width: 100%;
  background-color: rgb(236, 236, 236);
  height: 2rpx;
}
.drivelineWrap{
  width: 100%;
  display: flex;
  margin: 40rpx 0;
  justify-content: center;
}
.textarea-footer{
  display: flex;
  justify-content: space-between;
}
.mic-circle{
    width: 50rpx;
    background-color: #f1e1d4;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 15rpx;
    border-radius: 50%;
}
/* 分类选择面板 */
/* 容器设置为相对定位 */
.category-selection-container {
  flex: 0.8;
  position: relative; 
  height: 120rpx;
}

.category-tag-large {
  width: 100%;
  height: 100%;
  color: #FFFFFF;
  border-radius: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  font-weight: 600;
  box-shadow: 0 10rpx 25rpx rgba(74, 69, 62, 0.2);
  transition: all 0.3s;
}

.arrow-down {
  font-size: 32rpx;
  transition: transform 0.3s;
}
.arrow-down.rotate { transform: rotate(180deg); }

/* 下拉列表样式 */
.type-dropdown-list {
  position: absolute;
  top: 130rpx; /* 距离按钮底部一点距离 */
  left: 0;
  width: 100%;
  max-height: 0; /* 初始高度为0 */
  background: #FFFFFF;
  border-radius: 32rpx;
  box-shadow: 0 15rpx 40rpx rgba(0, 0, 0, 0.15);
  z-index: 100;
  overflow: hidden;
  transition: all 0.3s ease-in-out;
  opacity: 0;
  border: 1rpx solid #F0EEEB;
}

/* 显示状态 */
.type-dropdown-list.show {
  max-height: 450rpx; /* 设置最大高度，超出会滚动 */
  opacity: 1;
}

.dropdown-scroll {
  max-height: 450rpx;
  padding: 10rpx 0;
}

/* 列表项：横向并排 */
.dropdown-item {
  display: flex;
  align-items: center;
  padding: 24rpx 30rpx;
  transition: background 0.2s;
}

.dropdown-item:active {
  background-color: #F8F7F5;
}

.item-icon-circle {
  width: 50rpx;
  height: 50rpx;
  border-radius: 14rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 20rpx;
}

.item-icon-circle .material-icons {
  font-size: 30rpx;
}

.item-name {
  flex: 1;
  font-size: 28rpx;
  color: #44403C;
  font-weight: 500;
}

.check-mark {
  font-size: 32rpx;
  color: #A98467;
}